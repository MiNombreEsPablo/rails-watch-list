<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.input :movie_id, as: :select, collection: Movie.all, label_method: :title, value_method: :id %>
  <%= f.input :comment %>
  <%= f.submit %>
<% end %>
